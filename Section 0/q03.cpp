#include <iostream>

int main()
{
    std::cout << "A single tab is (\t)." << std::endl;
    std::cout << "It seems that it advances to the " <<
        "next tab stop, which is 8 spaces in our case." << std::endl;
    return 0;
}